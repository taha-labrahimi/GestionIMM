@model GestionIMM.Models.TransactionVente

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TransactionVente</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="Id" />
            <div class="form-group">
                <label asp-for="ProprieteId" class="control-label"></label>
                <select asp-for="ProprieteId" asp-items="@(ViewData["ProprieteId"] as SelectList)" class="form-control">
                    <option value="">Sélectionnez une propriété</option>
                </select>
                <span asp-validation-for="ProprieteId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="AcheteurId" class="control-label"></label>

                <select asp-for="AcheteurId" asp-items="@(ViewData["AcheteurId"] as SelectList)" class="form-control">
                    <option value="">Sélectionnez un Acheteur</option>
                </select>
                <span asp-validation-for="AcheteurId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="DateTransaction" class="control-label"></label>
                <input asp-for="DateTransaction" class="form-control" />
                <span asp-validation-for="DateTransaction" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Montant" class="control-label"></label>
                <input asp-for="Montant" class="form-control" />
                <span asp-validation-for="Montant" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
